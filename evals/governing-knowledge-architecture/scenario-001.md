# governing-knowledge-architecture evaluation: reusable UID architecture

Date: 2026-08-20

## Baseline

The agent produced a sophisticated UID-first authority model, but called the stronger profile “Strict,” expanded Core with many deployment-specific details, and had no exact VGTREE audit/plan boundary.

## With the skill

The agent converged on the public `Core` and `Governed` names, kept one authority model, defined the minimal Core contract and Governed increments, separated registries from projections, preserved UID through moves, treated source deletion as a separate transaction, and supplied exact read-only audit/plan commands with output outside the vault.

It also correctly stated that VGTREE v1 does not apply architecture changes to existing workspaces and that `scaffold` is not migration authorization.

Verdict: PASS.
