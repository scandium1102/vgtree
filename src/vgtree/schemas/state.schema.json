{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vgtree.dev/schemas/state.schema.json",
  "title": "VGTREE Workflow State",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "engine_version",
    "workflow_ref",
    "task",
    "task_class",
    "route",
    "phase",
    "branches",
    "evidence",
    "history"
  ],
  "properties": {
    "schema_version": { "const": "2.0" },
    "engine_version": { "type": "string", "minLength": 1 },
    "workflow_ref": { "const": "WF-VEGA-TREE@1.0" },
    "task": { "type": "object" },
    "task_class": { "enum": ["T0", "T1", "T2", "T3", "T4"] },
    "route": { "enum": ["direct", "specialized", "tree"] },
    "phase": {
      "enum": [
        "mission_understanding",
        "outcome_definition",
        "breadth_mapping",
        "branch_execution",
        "integration",
        "verification",
        "complete"
      ]
    },
    "branches": {
      "type": "array",
      "items": { "$ref": "#/$defs/branch" }
    },
    "evidence": {
      "type": "array",
      "items": { "$ref": "#/$defs/evidence" }
    },
    "history": {
      "type": "array",
      "items": { "$ref": "#/$defs/history" }
    }
  },
  "$defs": {
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type", "subject", "method", "timestamp", "outcome"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "type": { "type": "string", "minLength": 1 },
        "subject": { "type": "string", "minLength": 1 },
        "method": { "type": "string", "minLength": 1 },
        "timestamp": { "type": "string", "format": "date-time" },
        "outcome": { "enum": ["PASS", "FAIL", "REVIEW_REQUIRED", "BLOCKED"] },
        "digest": { "type": "string", "minLength": 1 },
        "reference": { "type": "string", "minLength": 1 }
      }
    },
    "limitation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["scope", "consequence", "owner", "accepted_at"],
      "properties": {
        "scope": { "type": "string", "minLength": 1 },
        "consequence": { "type": "string", "minLength": 1 },
        "owner": { "type": "string", "minLength": 1 },
        "accepted_at": { "type": "string", "format": "date-time" }
      }
    },
    "branch": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "title", "kind", "priority", "status", "depends_on", "evidence"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "title": { "type": "string", "minLength": 1 },
        "kind": { "enum": ["primary", "secondary"] },
        "priority": { "enum": ["P0", "P1", "P2", "DEFERRED"] },
        "status": {
          "enum": [
            "PENDING",
            "IN_PROGRESS",
            "VERIFIED",
            "BLOCKED",
            "ACCEPTED_LIMITATION"
          ]
        },
        "depends_on": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "definition_of_done": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1,
          "uniqueItems": true
        },
        "evidence_requirements": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1,
          "uniqueItems": true
        },
        "stop_condition": { "type": "string", "minLength": 1 },
        "evidence": {
          "type": "array",
          "items": { "$ref": "#/$defs/evidence" }
        },
        "blocked_reason": { "type": "string", "minLength": 1 },
        "limitation": { "$ref": "#/$defs/limitation" }
      }
    },
    "history": {
      "type": "object",
      "additionalProperties": false,
      "required": ["from", "to", "timestamp", "reason"],
      "properties": {
        "from": { "type": ["string", "null"] },
        "to": { "type": "string", "minLength": 1 },
        "timestamp": { "type": "string", "format": "date-time" },
        "reason": { "type": "string", "minLength": 1 }
      }
    }
  }
}
